<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="commu">
	
	<!-- 시퀀스 번호 가져오기 -->
	<select id="sequence">
		select commu_seq.nextval from dual
	</select>
	
	<!-- 글쓰기 -->
	<insert id="write" parameterType="commuDto">
		insert into commu(
			commu_no, member_no, middleanme,
			title, content, regdate, hit, viewwyn
			<if test="mapNo>0">
				, map_no,
			</if>
		)
		values(
			#{commuNo}, #{memberNo},#{middlename},
			#{title}, #{content},#{regdate},
			#{hit}, #{viewYN}
			<if test="mapNo>0">
				, #{mapNo},
			</if>
		)
	</insert>
	
	<!-- 상세조회 -->
	<select id="detail" parameterType="int" resultType="commuDTO">
		select * from commu where commu_no = #{commuNo}
	</select>
	
	<!-- 메뉴별 목록 조회 -->
	<select id="menuList" parameterType="String">
		select * from commu where middle_name = #{middlename}
	</select>
	
	<!-- 전체 글 목록 조회 -->
	<select id="list">
		select * from commu
	</select>
	
	<!-- 수정하기 -->
	<update id="change">
		update commu set
			title = #{titel}, content = #{content}
		where
			commu_no = #{commuNo}
	</update>
	
	<!-- 숨김처리 -->
	<update id="hide">
		update comu set
			viewyn = #{viewYN}
		where commu_no = #{commuNo}
	</update>
</mapper>